# API Configuration
API_BASE_URL=http://localhost:8000

# Streamlit Configuration
STREAMLIT_SERVER_PORT=8501
STREAMLIT_SERVER_ADDRESS=0.0.0.0

# Optional: Authentication (if implementing later)
# AUTH0_DOMAIN=your-domain.auth0.com
# AUTH0_CLIENT_ID=your-client-id
# AUTH0_CLIENT_SECRET=your-client-secret

# Email Service Configuration (Optional - for password reset and notifications)
# If not set, email service will use console logging for development
# SENDGRID_API_KEY=your-sendgrid-api-key-here
# FROM_EMAIL=noreply@yourdomain.com
# FROM_NAME=Your App Name
# ENVIRONMENT=production  # Set to "development" to enable debug mode (shows tokens/links in responses)

